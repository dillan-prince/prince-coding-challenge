import React from 'react';

const SearchBar = () => {
    return (
        <div className="SearchBar" style={{ border: '2px solid red' }}>
            SearchBar
        </div>
    );
};

export default SearchBar;
